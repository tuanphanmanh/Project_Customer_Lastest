//var validclose = false;
//window.onbeforeunload = function (e) {
//    if (!validclose) {
//        onclose(); 
//    }
//}

//function onclose() {
//    //window.open("/Logout.ascx", '_blank');
//} // 


//$(document).ready(function () {
//    $(document).on('keypress', function (e) { if (e.keyCode == 116) { validclose = true; } }); // Attach the event keypress to exclude the F5 refresh
//    $(document).on("click", "a", function () { validclose = true; }); // Attach the event click for all links in the page
//    $(document).on("submit", "form", function () { validclose = true; });  // Attach the event submit for all forms in the page
//    $(document).bind("click", "input[type=submit]", function () { validclose = true; });   // Attach the event click for all inputs in the page
//    $(document).bind("click", "button[type=submit]", function () { validclose = true; });
//});



